abstract final class ReminderDefaults {
  /// Days after a service before the next one is due.
  static const nextServiceDays = 90;

  /// Days before the due date to flag a customer as "due soon".
  static const dueSoonThresholdDays = 7;
}
