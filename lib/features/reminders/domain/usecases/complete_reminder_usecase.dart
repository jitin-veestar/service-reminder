// Completing a reminder means recording a new service — handled by the
// services feature. Placeholder for a future direct-completion flow.

import 'package:service_reminder/features/reminders/domain/repositories/reminders_repository.dart';

class CompleteReminderUseCase {
  // ignore: unused_field
  final RemindersRepository _repository;
  const CompleteReminderUseCase(this._repository);
}
