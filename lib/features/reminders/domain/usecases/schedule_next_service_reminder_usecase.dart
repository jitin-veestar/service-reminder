// Push notification scheduling is out of MVP scope.
// Placeholder for a future local-notification integration.

import 'package:service_reminder/features/reminders/domain/repositories/reminders_repository.dart';

class ScheduleNextServiceReminderUseCase {
  // ignore: unused_field
  final RemindersRepository _repository;
  const ScheduleNextServiceReminderUseCase(this._repository);
}
