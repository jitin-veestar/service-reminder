// Out of MVP scope — placeholder for future service status management.
import 'package:service_reminder/features/services/domain/repositories/services_repository.dart';

class UpdateServiceStatusUseCase {
  // ignore: unused_field
  final ServicesRepository _repository;
  const UpdateServiceStatusUseCase(this._repository);
}
