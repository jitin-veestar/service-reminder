// Out of MVP scope — placeholder for future in-visit checklist editing.
import 'package:service_reminder/features/services/domain/repositories/services_repository.dart';

class UpdateChecklistUseCase {
  // ignore: unused_field
  final ServicesRepository _repository;
  const UpdateChecklistUseCase(this._repository);
}
